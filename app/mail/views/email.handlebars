<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="x-ua-compatible" content="ie=edge" />
    <title>Reset Password</title>
    <style>
      body {
        font-family: Arial, Helvetica, sans-serif;
      }
      button {
        background-color: #008cba;
        border: none;
        color: white;
        padding: 15px 32px;
        text-align: center;
        text-decoration: none;
        display: inline-block;
        border-radius: 4px;
        box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
      }
      .card {
        padding: 2.5%;
        box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
        transition: 0.3s;
        border-radius: 5px;
      }
      p {
        color: rgba(0, 0, 0, .6);
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="card">
        <h3 class="card-header">Password Reset!</h3>
        <div class="card-body">
          <h4 class="card-title">Hello,</h4>
          <p class="card-text">
            You are receiving this email because we received a password request
            for your account
          </p>
          <button type="submit">
            <a style="text-decoration: none; color: white" href="{{link}}"
              >Reset Password</a
            >
          </button>
          <p class="card-text">
            If you did not request a password reset, no further action is
            required
          </p>
          <p>
            If you are having trouble clicking the "Reset Password" button, copy and paste the URL below into your web browser:
          </p>
          <a href="{{link}}">{{link}}</a>
        </div>
      </div>
    </div>
  </body>
</html>